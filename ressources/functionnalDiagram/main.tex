\documentclass{article}
\usepackage{tikz}
\usetikzlibrary{arrows.meta, positioning, shapes.geometric}
% Delete page number
\pagenumbering{gobble}



\begin{document}

\begin{tikzpicture}
    \tikzset{
        mynode/.style={
            rectangle, draw=blue!40, fill=blue!20, line width=1.5pt, minimum width=2cm, minimum height=1cm
        },
        Error/.style={
            diamond, draw=purple, fill=white, line width=1.5pt, minimum width=2.5cm, minimum height=1.5cm
        },
        myarrow/.style={
            ->, line width=1.2pt
        },
        note/.style={
            draw=none, fill=none, align=center, font=\footnotesize
        }
      }

      % Elements
      \node[mynode] (start) {\Large Start};
      \node[mynode, right=1cm of start] (main) {\Large int main(char argc, char *argv[])};
      \node[mynode, below=2cm of main] (sortParam) {\Large void sortParameters()};
      \node[mynode, left=2cm of sortParam] (isParam) {\Large bool isParam(char *arg)};
      \node[note, below=2cm of sortParam] (struct) {Parameters Struct : \\
        char *filePath \\
        char *outPutPath \\
        char *currentDirectory \\
        char **parameters};
      \node[mynode, below=2cm of struct] (checkingPath) {\Large bool checkingPath()};
      \node[mynode, below=2cm of checkingPath] (applyUserSelect) {\Large ApplyingUserSelection()};
      \node[mynode, below=2cm of applyUserSelect] (file) {\Large file.c};
      \node[mynode, left=1cm and 1cm of file] (metadata) {\Large metadata.c};
      \node[mynode, right=1cm and 1cm of file] (watermark) {\large watermark.c};

      % Links between elements
      \draw[myarrow] (start) -- (main);
      \draw[myarrow] (main) -- (sortParam);
      \draw[myarrow, bend left] (sortParam) to (isParam);
      \draw[myarrow, bend left] (isParam) to (sortParam);
      \draw[myarrow] (sortParam) -- (struct);
      \draw[myarrow, bend right=70] (struct) to (main);
      \draw[myarrow, bend left=70] (main) to (checkingPath);
      \draw[myarrow] (checkingPath) to (applyUserSelect);
      \draw[myarrow] (applyUserSelect) to (file);
      \draw[myarrow] (file) to (metadata);
      \draw[myarrow] (file) to (watermark);


\end{tikzpicture}

\end{document}
